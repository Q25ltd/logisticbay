{
  "name": "logisticbay",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n API,WEB -c blue,green \"npm run dev --prefix api\" \"npm run dev --prefix web\"",
    "dev:api": "npm run dev --prefix api",
    "dev:web": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "typecheck": "npx concurrently -n API,WEB -c blue,green \"tsc --noEmit --project api/tsconfig.json\" \"tsc --noEmit --project web/tsconfig.json\"",
    "deploy": "npm run build && npx vercel --prod"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
