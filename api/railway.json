{
  "$schema": "https://schema.railway.app/railpack.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "/bin/sh ./start.sh",
    "preDeployCommand": "npx prisma db push --schema=./prisma/schema.prisma --accept-data-loss"
  }
}
