{
  "outputDirectory": "web/dist",
  "installCommand": "cd web && npm install",
  "buildCommand": "cd web && npm run build",
  "ignoreCommand": "echo \"$VERCEL_GIT_COMMIT_REF\" | grep -q '^cleanup/'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
